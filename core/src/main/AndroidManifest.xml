<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>

        <provider
            android:name="com.arc.fast.core.FastCoreInitProvider"
            android:authorities="${applicationId}.arc.fast.core.init.provider"
            android:exported="false" />

    </application>

</manifest>